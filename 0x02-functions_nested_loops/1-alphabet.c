#include "main.h"

/**
 * main - entry point
 *
 * print_alphabet - print the alpha
 *
 * Return: Always(0) success
 */
void print_alphabet(void)
{
	char l;
	for (l = 'a'; l <= 'z'; l++)

		_putchar(l);
		_putchar('\n');
		return (0);
}
